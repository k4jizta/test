1. Настройте IP-адресацию по таблице
2. Настройте и проверьте динамический NAT

Router(config)# ip nat pool MerionNetworksPool 208.165.100.5 208.165.100.15 netmask 255.255.255.0
Router(config)# access-list 1 permit 192.168.0.0 0.0.255.255
Router(config)#ip nat inside source list 1 pool MerionNetworksPool
Router(config)# interface serial0/0/0
Router(config-if)#ip nat inside
Router(config-if)#exit
Router(config)# interface serial0/1/0
Router(config-if)#ip nat outside

3. На любом из устройств установите пароль на привилегированный режим

enable password __

4. На любом из устройств настройте SSH-сервер

  enable
  clock set 17:10:00 28 Aug 2009
  configure terminal
  ip domain name test.dom
  crypto key generate rsa
  service password-encryption
  username user privilege 15 password 7 Pa$$w0rd
  aaa new-model
  line vty 0 4
    transport input ssh
    logging synchronous
    exec-timeout 60 0
